<UserControl x:Class="AeroCut.CropEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="Board" Background="Black" ClipToBounds="True" SizeChanged="OnSizeChanged">
        <Grid.Resources>
            <Style x:Key="Corner" TargetType="Thumb">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="{DynamicResource AccentFillColorDefaultBrush}"
                                    BorderThickness="2" CornerRadius="3"
                                    BorderBrush="{DynamicResource TextFillColorPrimaryBrush}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Canvas x:Name="Layer">
            <Image x:Name="Frame" Stretch="Fill" />
            <Thumb x:Name="CropBody" Cursor="SizeAll">
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#22FFFFFF" BorderThickness="2"
                                BorderBrush="{DynamicResource AccentFillColorDefaultBrush}" />
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb x:Name="TL" Width="16" Height="16" Cursor="SizeNWSE" Style="{StaticResource Corner}" />
            <Thumb x:Name="TR" Width="16" Height="16" Cursor="SizeNESW" Style="{StaticResource Corner}" />
            <Thumb x:Name="BL" Width="16" Height="16" Cursor="SizeNESW" Style="{StaticResource Corner}" />
            <Thumb x:Name="BR" Width="16" Height="16" Cursor="SizeNWSE" Style="{StaticResource Corner}" />
        </Canvas>
    </Grid>
</UserControl>
